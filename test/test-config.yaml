#
# Test config file
#
disableContainerImagesRelocation: true
source:
  repo:
    kind: "HELM"
    url: "https://charts.bitnami.com/bitnami"
target:
  containerRegistry: "gcr.io"
  containerRepository: "bitnami-containers"
  repo:
    kind: "CHARTMUSEUM"
    url: "http://127.0.0.1:8080"
    auth:
      username: "admin"
      password: "dummypassword"
charts:
  - ghost
