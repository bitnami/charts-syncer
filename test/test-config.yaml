#
# Test config file
#
source:
  repo:
    kind: "OCI"
    url: "https://registry-1.docker.io/bitnamicharts"
target:
  containers:
    auth:
      username: admin
      password: dummypassword

  repo:
    kind: "OCI"
    url: "http://localhost:8080/library"
    auth:
      username: "admin"
      password: "dummypassword"

charts:
  - ghost
