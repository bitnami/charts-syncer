#
# Test config file
#
source:
  repo:
    kind: "OCI"
    url: "http://localhost:5001/library"
target:
  containers:
    auth:
      username: admin
      password: dummypassword

  repo:
    kind: "OCI"
    url: "http://localhost:5000/library"
    auth:
      username: "admin"
      password: "dummypassword"

charts:
  - wordpress
