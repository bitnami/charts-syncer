#
# Test config file
#
source:
  repo:
    kind: "HELM"
    url: "https://charts.bitnami.com/bitnami"
target:
  containers:
    auth:
      username: admin
      password: dummypassword

  repo:
    kind: "OCI"
    url: "http://localhost:8080/library"
    auth:
      username: "admin"
      password: "dummypassword"

charts:
  - ghost
